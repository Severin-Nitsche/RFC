I am an excellent linguist. The task is to verify whether an entity extracted from the given sentence is ${entity_kind}.
${explanations}

The given sentence: ${example[0].input}
Is the entity "${example[0].entity}" ${entity_kind}? Please answer with ${options}.
${example[0].kind}

The given sentence: ${example[1].input}
Is the entity "${example[1].entity}" ${entity_kind}? Please answer with ${options}.
${example[1].kind}

The given sentence: ${example[2].input}
Is the entity "${example[2].entity}" ${entity_kind}? Please answer with ${options}.
${example[2].kind}

The given sentence: ${prompt.input}
Is the entitiy "${prompt.entity}" ${entity_kind}? Please anser with ${options}.
